# `chat/` data-bearing charter (NBB-204)

**Owner:** NBB-204 owns the data-bearing overlay for this root: which tables, which RLS/guard invariants, which JSONB contracts belong here. The package-level import/dependency charter in `__init__.py` (authored by NBB-104) still applies; read it first.

**Validation approach:** every table, column, or JSONB field below must point at a migration file. Every access-guard claim must point at a route or service file. Reviewer uses `backend/supabase/migrations/OWNERS.md` to spot-check.

**Migration source (NBB-209A):** chat and message stores currently live under `backend/app/services/data_services/`; `NBB-209A` moves them here after `NBB-109`'s chat N+1/cost fixes land.

## Tables owned by `chat/`

| Table | Defined in | Access enforcement | Notes |
|---|---|---|---|
| `chats` | `backend/supabase/migrations/00001_initial_schema.sql`, RLS in `00003_rls_policies.sql` | Hosted: RLS via `chats.project_id -> projects.user_id` subquery. Self-hosted: backend guard (`has_project_access` run before any `/projects/{id}/chats/...` route). Both layers in hosted mode. | Per-chat source selection lives in `selected_source_ids` (00013). Per-chat cost mirror lives in `costs` (00018). |
| `messages` | `00001_initial_schema.sql`, RLS in `00003_rls_policies.sql` | Hosted: RLS via `messages.chat_id -> chats.project_id -> projects.user_id`. Self-hosted: backend guard via chat route entry. | `content` JSONB stores Claude content blocks; role enum is `user | assistant | tool_use | tool_result`. |

## Columns / JSONB contracts owned here

| Column | Table | Defined in | Purpose / contract owner |
|---|---|---|---|
| `chats.title` | `chats` | 00001 | Auto-generated by background naming task (NBB-210 / background owner). |
| `chats.selected_source_ids` | `chats` | `00013_chat_selected_sources.sql` | Per-chat source selection. `NULL` = legacy fallback to all ready+active sources; empty array = no sources; non-empty = explicit selection. Contract shape owner: NBB-205. |
| `chats.costs` | `chats` | `00018_chat_costs.sql` | Mirrors `projects.costs` shape; updated in the same locked operation. Contract shape owner: NBB-205. |
| `messages.content` | `messages` | 00001 | Claude content blocks in JSONB. Contract shape owner: NBB-205. |
| `messages.citations` | `messages` | 00001 | `TEXT[]` of chunk IDs. Chunk ID format is defined by `sources/` (see `sources/CHARTER.md`). |
| `messages.model`, `messages.tokens_input`, `messages.tokens_output`, `messages.cost_usd` | `messages` | 00001 | Per-message API metadata used for cost aggregation. |

## Access guard of record

- Entry guard: `@before_request` enforcement in `backend/app/__init__.py` runs `project_service.has_project_access(project_id, user_id)` before any `/api/v1/projects/{id}/chats/...` or `/api/v1/projects/{id}/chats/{chat_id}/messages` route handler runs.
- RLS defence-in-depth (hosted): `chats` and `messages` policies in `00003_rls_policies.sql` use a project-ownership subquery. A missed `has_project_access` in a new chat route would still be blocked at the DB layer in hosted deployments.
- Self-hosted mode has no RLS on these tables (single-user Docker). The project guard is the only barrier; any new chat route that takes a `project_id` path segment inherits it.

## Data-move pre-flight (NBB-209A and any future chat-store migrations)

1. Keep the RLS subquery shape unchanged when moving the store; any rename that breaks the `chat_id -> project_id` join must be staged as a dedicated migration ticket.
2. NBB-302 split the legacy `main_chat_service.py` into `chat/loop.py`, `chat/context.py`, `chat/tool/policy.py`, `chat/persistence.py`, `chat/stream.py`, `chat/memory/`, and `chat/naming.py`; the existing ownership query path is preserved. Do not introduce new direct reads of `messages` from outside `chat/` internals.
3. Chat-invoked tools (NBB-303) may store rows in other domain tables (memory, studio signals, connector invocations). Those rows remain owned by the target domain's charter, not this one.

## Cross-reference

- Import/dependency charter: `backend/app/chat/__init__.py`.
- Schema/RLS inventory: `backend/supabase/migrations/OWNERS.md`.
- Chat contract shapes (streaming events, citation markers, tool invocation): `NBB-205`.
- Access smoke checklist: `docs/tickets/checklists/data_access_smoke.md`.
