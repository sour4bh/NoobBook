"""
Legacy API-key validator body package.

NBB-805 moved runtime settings ownership to `app.settings.env` and the
validation dispatcher to `app.settings.validation`. This package remains only
for individual validator bodies until NBB-806/NBB-807 move them to
provider/connector homes.
"""
