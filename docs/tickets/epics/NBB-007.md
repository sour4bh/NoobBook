---
key: NBB-007
type: Epic
title: Verification, Enforcement, and Cleanup
phase: Phase 6 - Deep verification and cleanup
priority: P1
---

<a id="nbb-007"></a>
# NBB-007 - Verification, Enforcement, and Cleanup

## Goal

Deepen tests after owning migrations land, enforce the new structure, drain approved utility moves, and remove migration leftovers.

<a id="nbb-701"></a>
### NBB-701 - Add chat tool contract tests

**Goal:** Protect chat tool routing and result contracts after migration.

**Primary write scope:**
- backend chat/tool tests
- `backend/app/chat/`
- `backend/app/auth/`
- `backend/app/connectors/`
- `backend/app/sources/`

**Scope:**
- Add tests for tool availability, permission filtering, tool invocation/result shape, streaming behavior, and persistence interaction.
- Create `backend/tests/test_main_chat_service_tool_loop.py` or the migrated equivalent. Pin this invariant: every `tool_use` content block in assistant turn N is matched by a `tool_result` content block with the same `tool_use_id` in user turn N+1.
- Cover both success and executor-error paths; an executor exception still produces a matching `tool_result` error payload rather than dropping the pair.
- Cover both public chat entrypoints from `NBB-301`: `chat.send(...)` and `chat.stream(...)`.

**Out of scope:**
- New chat features.
- Tool behavior changes.

**Acceptance criteria:** Chat/tool regressions fail before cleanup removes shims.

**Verification:** Backend chat/tool tests pass.

**Traceability:** Covers chat contract risk.

---

<a id="nbb-702"></a>
### NBB-702 - Add source citation and analysis tests

**Goal:** Protect source, citation, and analysis behavior after migration.

**Primary write scope:**
- backend source/citation tests
- backend analysis/security tests
- `backend/app/sources/`
- `backend/app/sources/analysis/`

**Scope:** Add tests for citation markers/lookups, source status/kinds, ingestion smoke paths, and analysis boundaries.
- Test the `NBB-203` quarantine behavior: raw-code analysis is disabled/flag-gated correctly.
- If a future program activates the permanent raw-code replacement from `D-002`, that program updates this ticket or adds replacement-boundary tests.

**Out of scope:**
- New analysis capabilities.
- Ingestion behavior rewrites.
- Permanent raw-code replacement; that is deferred in `D-002`.

**Acceptance criteria:** Citation and source-analysis contract drift fails tests.

**Verification:** Backend source tests pass.

**Traceability:** Covers source/citation/analysis safety.

---

<a id="nbb-703"></a>
### NBB-703 - Add studio job background status tests

**Goal:** Protect studio job lifecycle, progress, and status behavior.

**Primary write scope:**
- backend studio/background tests
- `backend/app/studio/`
- `backend/app/background/`
- `backend/app/api/projects/active_tasks.py`

**Scope:** Add tests for studio job creation/status/progress/result shape, background task linkage, cancellation, and active-task polling.

**Out of scope:**
- New studio items.
- Queue redesign.

**Acceptance criteria:** Studio/background status drift fails tests.

**Verification:** Backend studio/background tests pass.

**Traceability:** Covers studio job/background contracts.

---

<a id="nbb-704a"></a>
### NBB-704A - Add early architecture checks

**Goal:** Enforce root and dependency-direction basics before full migration completes.

**Primary write scope:**
- architecture check script
- `.github/workflows/`
- backend root/dependency config
- docs that explain structural checks

**Scope:**
- Add early checks for approved backend roots and dependency-direction basics from `NBB-104` and `NBB-206`.
- Keep checks broad enough to catch obvious new legacy imports without requiring all migrations to be complete.
- Keep checks maintainable and readable.

**Out of scope:**
- Full post-migration import-boundary coverage.
- Custom lint-framework authorship.

**Acceptance criteria:**
- CI fails on high-value root or dependency-direction violations.
- Checks do not block known migration shims.

**Verification:** CI and local architecture checks pass.

**Traceability:** Covers early dependency-direction enforcement.

---

<a id="nbb-704b"></a>
### NBB-704B - Add rich post-migration import-boundary checks

**Goal:** Prevent migrated domains from regressing into old buckets or cross-domain internals.

**Primary write scope:**
- architecture check script
- backend import-boundary config
- frontend ownership check config
- `.github/workflows/`

**Scope:**
- Add richer import-boundary checks after domains exist.
- Enforce dependency direction from `NBB-104`, `NBB-206`, and migrated domain public surfaces.
- Include frontend ownership checks after `NBB-603`.
- Keep checks maintainable and readable.

**Out of scope:**
- Style-only lint rules.
- Custom lint-framework authorship.

**Acceptance criteria:**
- CI fails on high-value dependency violations.
- Checks do not require a heavy custom framework.

**Verification:** CI and local architecture checks pass.

**Traceability:** Covers dependency-direction enforcement.

---

<a id="nbb-704c"></a>
### NBB-704C - Add type and AST safety checks

**Goal:** Add the audit's Phase 5 safety checks before final cleanup deletes shims and barrels.

**Primary write scope:**
- `pyrightconfig.json`
- `.github/workflows/`
- `backend/scripts/verify_project_id_coverage.py`
- `backend/scripts/verify_no_stateless_singletons.py`
- type/AST check docs

**Scope:**
- Check in `pyrightconfig.json` at repo root. Start with `include: ["backend/app"]`, `typeCheckingMode: "standard"`, and selected strict/current-owner paths: `backend/app/chat/tools`, `backend/app/chat/loop.py`, `backend/app/sources/analysis`, `backend/app/studio/**/tool.py`, `backend/app/studio/**/run.py`, `backend/app/connectors`, and `backend/app/providers`.
- Run pyright through uvx with a pinned command: `uvx --from pyright==1.1.409 pyright`. Do not add pyright to `backend/requirements.txt`.
- Wire `backend/scripts/verify_project_id_coverage.py` from `NBB-109` into CI.
- Add `backend/scripts/verify_no_stateless_singletons.py`: an AST check that flags new `class *Service` or `class *Executor` definitions with empty `__init__` plus a module-level singleton assignment.
- The stateless-singleton checker may carry a temporary allowlist for the seven explicit `NBB-706` conversions. `NBB-706` must drain that allowlist or document any remaining stateful exception.

**Out of scope:**
- Making all backend code strict immediately.
- Adding tracked Python type-checking dependencies.
- Replacing import-boundary checks from `NBB-704A` or `NBB-704B`.

**Acceptance criteria:**
- CI runs pyright with the checked-in config and pinned command.
- CI runs both AST safety scripts.
- New stateless singleton service/executor classes fail checks unless explicitly allowlisted with an owning cleanup ticket.

**Verification:** Run `uvx --from pyright==1.1.409 pyright`, `uv run python backend/scripts/verify_project_id_coverage.py`, and `uv run python backend/scripts/verify_no_stateless_singletons.py`.

**Traceability:** Covers audit Phase 5 type hints, AST verifier, pyright, and stateless-singleton prevention.

---

<a id="nbb-705a"></a>
### NBB-705A - Drain auth utilities

**Goal:** Move auth utilities after auth ownership lands.

**Primary write scope:**
- `backend/app/utils/auth_middleware.py`
- `backend/app/utils/password_utils.py`
- `backend/app/api/auth/middleware.py`
- `backend/app/auth/password.py`

**Scope:**
- Confirm `utils/auth_middleware.py` lands through `NBB-201` at `api/auth/middleware.py`.
- Confirm `utils/password_utils.py` lands through `NBB-209C` at `auth/password.py`.
- Remove old utility imports or leave only scheduled shims for `NBB-706`.
- Run `mcp__refactory__validate_imports` on `backend/app` to confirm `NBB-201` and `NBB-209C` left no stale imports, and run `docs/tickets/helpers/string_ref_scan.py` against each drained path. Append one `text_reference_check` row per drained path to `docs/tickets/move-plan.csv` with `ticket=NBB-705A`.
- Ownership divergence from the audit is intentional: the audit placed `password_utils.py` next to `user_service` under `data_services/`; this ticket places it under `auth/` because `NBB-209C` moves `UserStore` to `auth/user/store.py`, and password helpers follow user/auth ownership.

**Out of scope:**
- Auth behavior redesign.

**Acceptance criteria:**
- Auth utilities are no longer owned by generic `utils/`.
- Remaining shims have explicit removal path.
- Per-move post-condition: `rg 'from app\\.utils\\.<old_name>' backend/app` returns zero for `auth_middleware` and `password_utils`.

**Verification:** Search old auth utility imports and run auth tests.

**Traceability:** Covers auth utility ownership.

---

<a id="nbb-705b"></a>
### NBB-705B - Drain source utilities

**Goal:** Move source utilities after source owners exist.

**Primary write scope:**
- `backend/app/utils/embedding_utils.py`
- `backend/app/utils/source_content_utils.py`
- `backend/app/utils/file_utils.py`
- `backend/app/utils/citation_utils.py`
- `backend/app/utils/pdf_utils.py`
- `backend/app/utils/pptx_utils.py`
- `backend/app/utils/docx_utils.py`
- `backend/app/utils/batching_utils.py`
- `backend/app/sources/`

**Decision map:**

| Current file | Target owner under `backend/app/` | Locked decision |
|---|---|---|
| `utils/embedding_utils.py` | `sources/tokens.py` plus `providers/anthropic/token_count.py` | Local tiktoken/chunk config stays source pipeline-owned; API token counting moves to Anthropic provider ownership. |
| `utils/source_content_utils.py` | `sources/content.py` | Source-content read helper. |
| `utils/file_utils.py` | `sources/file_contract.py` | Lands through `NBB-401`; owns source kind/MIME/size contract. |
| `utils/citation_utils.py` | `sources/citations.py` | Lands through `NBB-401`; preserves `[[cite:chunk_id]]`. |
| `utils/pdf_utils.py` | `sources/pdf/ops.py` | Lands through `NBB-401`. |
| `utils/pptx_utils.py` | `sources/pptx/ops.py` | Lands through `NBB-401`. |
| `utils/docx_utils.py` | `sources/docx/ops.py` | Lands through `NBB-401`. |
| `utils/batching_utils.py` | `sources/extract/batching.py` | Source extraction batch sizing for PDF/PPTX/image flows. |

Intentional divergences from the audit:
- The audit proposed format-repeating filenames such as `pdf_ops.py`; this ticket uses `sources/<format>/ops.py` because `sources/pdf/`, `sources/pptx/`, and `sources/docx/` already supply the format context.
- The audit placed `batching_utils.py` under `ai_services/`; this ticket places it under `sources/extract/` because the current callers are source-extraction flows. If a future provider-neutral batching user appears, promote the shared base through a new charter instead of pre-creating one here.

**Scope:**
- Move source-owned utilities by the decision map above.
- Do not replace `utils` with `base`, `shared`, `platform/files`, or `providers/files` as a dumping ground.
- Use `refactory:move_module` for whole-file drains and `refactory:move_symbol` for the tiktoken/chunk half of `utils/embedding_utils.py` (the API-token-counting half moves in `NBB-705C`). For every `move_symbol` row, `touch` the target file first — refactory's Python `move_symbol` dry-run requires the target module to exist on disk. Append one row per operation to `docs/tickets/move-plan.csv` with `ticket=NBB-705B`.

**Out of scope:**
- Helper behavior rewrites.
- Provider-owned API token counting move; that is covered by `NBB-705C`.

**Acceptance criteria:**
- Source utilities have source owners or approved exceptions.
- Source utility imports no longer depend on generic utility ownership.
- Per-move post-condition: `rg 'from app\\.utils\\.<old_name>' backend/app` returns zero for every drained source utility in the decision map.

**Verification:** Search source utility imports and run source tests.

**Traceability:** Covers source utility ownership contradictions.

---

<a id="nbb-705c"></a>
### NBB-705C - Drain provider and Anthropic utilities

**Goal:** Move Claude cost, token, and media helpers under provider ownership.

**Primary write scope:**
- `backend/app/utils/claude_parsing_utils.py`
- `backend/app/utils/cost_tracking.py`
- `backend/app/utils/embedding_utils.py`
- `backend/app/utils/rate_limit_utils.py`
- `backend/app/utils/encoding_utils.py`
- `backend/app/providers/anthropic/`

**Decision map:**

| Current move | Mode | Tool |
|---|---|---|
| `utils/claude_parsing_utils.py` → `providers/anthropic/response_parser.py` (symbols: `is_end_turn`, `is_tool_use`, `get_stop_reason`, `extract_text`, `extract_citations`, `extract_text_with_citations`, `extract_tool_use_blocks`, `extract_tool_inputs`, `extract_server_tool_use_blocks`, `extract_server_tool_results`, `has_server_tool_use`) | `python_symbol_move` | `refactory:move_symbol` |
| `utils/claude_parsing_utils.py` → `providers/anthropic/content.py` (symbols: `build_tool_result_content`, `build_single_tool_result`, `_serialize_anthropic_object`, `serialize_content_blocks`) | `python_symbol_move` | `refactory:move_symbol` |
| `utils/claude_parsing_utils.py` → `providers/anthropic/usage.py` (symbols: `get_token_usage`, `get_model`) | `python_symbol_move` | `refactory:move_symbol` |
| `utils/cost_tracking.py` → `providers/anthropic/cost.py` | `python_module_move` | `refactory:move_module` |
| `utils/embedding_utils.py` → `providers/anthropic/token_count.py` (symbol: `count_tokens_api`) | `python_symbol_move` | `refactory:move_symbol` |
| `utils/rate_limit_utils.py` → `providers/anthropic/rate.py` | `python_module_move` | `refactory:move_module` |
| `utils/encoding_utils.py` → `providers/anthropic/media.py` | `python_module_move` | `refactory:move_module` |

**Refactory call sequence (one refactory call per row; one `move-plan.csv` row per call):**

| Old path::symbol | New path::symbol | Mode | Tool |
|---|---|---|---|
| `utils/claude_parsing_utils.py::is_end_turn` | `providers/anthropic/response_parser.py::is_end_turn` | `python_symbol_move` | `refactory:move_symbol` |
| `utils/claude_parsing_utils.py::is_tool_use` | `providers/anthropic/response_parser.py::is_tool_use` | `python_symbol_move` | `refactory:move_symbol` |
| `utils/claude_parsing_utils.py::get_stop_reason` | `providers/anthropic/response_parser.py::get_stop_reason` | `python_symbol_move` | `refactory:move_symbol` |
| `utils/claude_parsing_utils.py::extract_text` | `providers/anthropic/response_parser.py::extract_text` | `python_symbol_move` | `refactory:move_symbol` |
| `utils/claude_parsing_utils.py::extract_citations` | `providers/anthropic/response_parser.py::extract_citations` | `python_symbol_move` | `refactory:move_symbol` |
| `utils/claude_parsing_utils.py::extract_text_with_citations` | `providers/anthropic/response_parser.py::extract_text_with_citations` | `python_symbol_move` | `refactory:move_symbol` |
| `utils/claude_parsing_utils.py::extract_tool_use_blocks` | `providers/anthropic/response_parser.py::extract_tool_use_blocks` | `python_symbol_move` | `refactory:move_symbol` |
| `utils/claude_parsing_utils.py::extract_tool_inputs` | `providers/anthropic/response_parser.py::extract_tool_inputs` | `python_symbol_move` | `refactory:move_symbol` |
| `utils/claude_parsing_utils.py::extract_server_tool_use_blocks` | `providers/anthropic/response_parser.py::extract_server_tool_use_blocks` | `python_symbol_move` | `refactory:move_symbol` |
| `utils/claude_parsing_utils.py::extract_server_tool_results` | `providers/anthropic/response_parser.py::extract_server_tool_results` | `python_symbol_move` | `refactory:move_symbol` |
| `utils/claude_parsing_utils.py::has_server_tool_use` | `providers/anthropic/response_parser.py::has_server_tool_use` | `python_symbol_move` | `refactory:move_symbol` |
| `utils/claude_parsing_utils.py::build_tool_result_content` | `providers/anthropic/content.py::build_tool_result_content` | `python_symbol_move` | `refactory:move_symbol` |
| `utils/claude_parsing_utils.py::build_single_tool_result` | `providers/anthropic/content.py::build_single_tool_result` | `python_symbol_move` | `refactory:move_symbol` |
| `utils/claude_parsing_utils.py::_serialize_anthropic_object` | `providers/anthropic/content.py::_serialize_anthropic_object` | `python_symbol_move` | `refactory:move_symbol` |
| `utils/claude_parsing_utils.py::serialize_content_blocks` | `providers/anthropic/content.py::serialize_content_blocks` | `python_symbol_move` | `refactory:move_symbol` |
| `utils/claude_parsing_utils.py::get_token_usage` | `providers/anthropic/usage.py::get_token_usage` | `python_symbol_move` | `refactory:move_symbol` |
| `utils/claude_parsing_utils.py::get_model` | `providers/anthropic/usage.py::get_model` | `python_symbol_move` | `refactory:move_symbol` |
| `utils/cost_tracking.py` | `providers/anthropic/cost.py` | `python_module_move` | `refactory:move_module` |
| `utils/embedding_utils.py::count_tokens_api` | `providers/anthropic/token_count.py::count_tokens_api` | `python_symbol_move` | `refactory:move_symbol` |
| `utils/rate_limit_utils.py` | `providers/anthropic/rate.py` | `python_module_move` | `refactory:move_module` |
| `utils/encoding_utils.py` | `providers/anthropic/media.py` | `python_module_move` | `refactory:move_module` |

Execute each row as one refactory call (`touch` the target module first for every `python_symbol_move`). If a split target turns out to need a different grouping — for example, a symbol's dependencies push it into a different sibling — revise this table before re-running refactory; do not re-decide at call time.

Locked decisions:
- Anthropic/Claude response parsing and content-block helpers belong to provider ownership, not chat. Splitting `claude_parsing_utils.py` into three sibling modules keeps usage/cost helpers separate so chat and orchestration code do not pick them up transitively.
- Sibling name divergence from the audit is intentional: the audit proposed `content_builder.py` and `token_usage.py`; this ticket uses `content.py` and `usage.py` because `providers/anthropic/` supplies provider context and the shorter names avoid ancestor repetition.
- `is_tool_use` lives in `response_parser.py` with the response-shape predicates, not in `content.py`; the predicate answers what the response is before any downstream content-building decision.
- `cost_tracking.py` preserves `projects.costs` and the chat-cost contract from `NBB-205`.
- The API-token-counting half of `embedding_utils.py` moves to provider ownership; the local tiktoken/chunk half stays source-pipeline-owned per `NBB-705B`.
- `rate_limit_utils.py` owns Anthropic request pacing used by source extraction. Divergence from the audit's `ai_services/rate_limit.py` target is intentional because this helper expresses provider API pacing, not a domain operation.
- `encoding_utils.py` owns base64/media helpers for Anthropic file/image messages. Divergence from the audit's `ai_services/encoding.py` target is intentional; if a non-Anthropic provider later needs media encoding, extract a provider-neutral base through a charter.

**Scope:**
- Move provider/Anthropic-owned utilities by the decision map above.
- Preserve cost and message contracts named by `NBB-205`.
- Keep runtime reload/app bootstrap constraints from `NBB-208A` visible.
- `claude_parsing_utils.py` and the API half of `embedding_utils.py` are split targets, not whole-file moves. For each `python_symbol_move` row, `touch` the target file first — refactory's Python `move_symbol` dry-run requires the target module to exist on disk. Execute each symbol move, review its dry-run diff, then apply. Delete `claude_parsing_utils.py` only after all three sibling symbols have landed and the source file is empty.
- Do not merge `response_parser.py`, `content.py`, and `usage.py` back into one module; the split exists so chat and orchestration code do not transitively import Anthropic usage/cost helpers.
- Known refactory limitation: `move_symbol` fails closed on circular imports among the three split targets. If `response_parser.py` and `content.py` end up cross-referencing, introduce a shared private module or extract the common symbol first.
- Append one row per operation to `docs/tickets/move-plan.csv` with `ticket=NBB-705C`.

**Out of scope:**
- Provider client redesign.
- Source-local tiktoken move; that is covered by `NBB-705B`.

**Acceptance criteria:**
- Anthropic helpers do not remain in generic `utils/`.
- Cost tracking contract remains compatible.
- Per-move post-condition: `rg 'from app\\.utils\\.<old_name>' backend/app` returns zero for every drained provider utility in the decision map.

**Verification:** Search provider utility imports and run provider/cost tests where available.

**Traceability:** Covers Claude parsing/cost/token utility ownership.

---

<a id="nbb-705d"></a>
### NBB-705D - Drain studio utilities

**Goal:** Move studio export, screenshot, and design utilities after relevant studio owners exist.

**Primary write scope:**
- `backend/app/utils/presentation_export_utils.py`
- `backend/app/utils/screenshot_utils.py`
- `backend/app/utils/excalidraw_utils.py`
- `backend/app/studio/export/`
- `backend/app/studio/design/wireframe/`

**Decision map:**

| Current file | Target owner under `backend/app/` | Locked decision |
|---|---|---|
| `utils/presentation_export_utils.py` | `studio/export/presentation.py` | Studio export ownership, not source/provider. |
| `utils/screenshot_utils.py` | `studio/export/screenshot.py` | Studio screenshot/export ownership, not source/provider. |
| `utils/excalidraw_utils.py` | `studio/design/wireframe/excalidraw.py` | Wireframe/design-owned conversion helper. |

**Scope:**
- Move studio-owned utilities by the decision map above.
- Keep `studio/export/` as a sanctioned studio capability group, not an unchartered `shared/` bucket.
- Use `refactory:move_module` for each whole-file move in the decision map. Append one row per operation to `docs/tickets/move-plan.csv` with `ticket=NBB-705D`.

**Out of scope:**
- Studio item behavior rewrites.

**Acceptance criteria:**
- Studio utilities have studio owners.
- No studio utility is parked in a generic helper bucket.
- Per-move post-condition: `rg 'from app\\.utils\\.<old_name>' backend/app` returns zero for every drained studio utility in the decision map.

**Verification:** Search studio utility imports and run studio/export tests where available.

**Traceability:** Covers studio utility ownership.

---

<a id="nbb-705e"></a>
### NBB-705E - Verify final utils exceptions

**Goal:** Confirm `utils` contains only approved exceptions or documented rehomes.

**Primary write scope:**
- `backend/app/utils/`
- utility exception docs
- architecture check allowlist

**Decision map:**

| Current file | Final decision |
|---|---|
| `utils/path_utils.py` | Approved exception unless explicitly rehomed later. Canonical cross-cutting filesystem path helper. |
| `utils/logger.py` | Approved exception unless explicitly rehomed later. Bootstrap logging helper. |
| `utils/text/` | Approved exception unless explicitly rehomed later. Cohesive text-processing package. |

**Scope:**
- Verify all utility drain slices have landed.
- Confirm final `utils/` contains only approved exceptions or documented rehomes.
- Do not require `utils/` to be empty.
- Add a top-of-file approved-exception comment to `backend/app/utils/path_utils.py`: `# Cross-cutting: canonical filesystem path helper. Approved exception to utils dissolution (NBB-705E). Do not re-home without updating NBB-705E and the dependency-direction rules in NBB-104.`

**Out of scope:**
- New generic bucket under another name.

**Acceptance criteria:**
- Every former util has an owner or approved exception.
- Final `utils/` state is allowed to contain only `path_utils.py`, `logger.py`, and `text/`.
- An empty `utils/` directory is not required unless the approved exceptions are explicitly rehomed.

**Verification:** Search `backend/app/utils` and review exceptions.

**Traceability:** Covers utility ownership contradictions and final exception policy.

---

<a id="nbb-706"></a>
### NBB-706 - Drop suffix drift, convert stateless classes, remove barrels, delete shims, update final docs

**Goal:** Make the new structure the current structure, not a compatibility layer over the old one.

**Primary write scope:**
- final architecture docs
- migrated backend package `__init__.py` files
- compatibility shims
- suffix/barrel cleanup checks

**Scope:**
- Verify that upstream migration tickets finished the renames and moves they own. `NBB-209A`–`NBB-209E` already rename data services to `*Store`; `NBB-303` already moves tool executors under owning domains; `NBB-502`/`NBB-504`–`NBB-507` already rename studio items per the verb table; `NBB-705A`–`NBB-705D` already move provider/source/studio utilities. `NBB-706` does not re-open any of those renames.
- Search the migrated tree for any residual `*Service`/`*Executor` class names. For each residual, document it as either (a) a legitimate exception (stateful class, explicit platform contract, or path that does not supply verb context) or (b) a holdout that an upstream ticket missed. Holdouts are fixed by reopening the owning upstream ticket or by a narrow manual rename in this ticket, logged in the ticket's PR description. `NBB-706` does not issue `refactory:rename_symbol` calls; the renames it cares about were supposed to land upstream, and any remaining drift is small enough that refactory adds no safety over a manual edit plus the standard post-apply checklist.
- Remove accidental `__init__.py` barrel re-exports. Audit baseline: 43 of 47 `__init__.py` files contain re-export tricks. Named target: remove `__all__` re-exports from `backend/app/config/__init__.py` for `tool_loader`, `prompt_loader`, and `tier_loader`; update call sites to import the concrete modules directly. Named exception: `services/integrations/knowledge_bases/` style barrels may remain only if they function as deliberate compatibility boundaries and are documented per-file.
- Delete temporary shims and forwarding-only modules unless a documented compatibility boundary remains.
- Convert only the seven verified stateless classes below where earlier moves left the class intact. This is a semantic change, stays manual, and is not tracked in `move-plan.csv`.
- Update final architecture docs to describe the new truth.

**Stateless conversion map:**

| Current class | Target module shape | Exposed symbols | Module-private state |
|---|---|---|---|
| `integrations/supabase/supabase_client.py::SupabaseClient` | Same module, class deleted | `get_client()`, `is_configured()`, `reset()` | `_client`, `_initialized` |
| `integrations/openai/openai_service.py::OpenAIService` | Rename module to `openai.py` if the provider path does not already supply that name | `create_embedding()`, `create_embeddings_batch()`, `get_embedding_dimensions()` | `_client` |
| `ai_services/embedding_service.py::EmbeddingService` | Class deleted in the migrated source/provider home | `process_embeddings()` | none |
| `app_settings/validation/validation_service.py::ValidationService` | Class deleted in the migrated settings validation home | `validate(key_name, value)` plus per-key sibling validator functions | none |
| `ai_services/summary_service.py::SummaryService` | Class deleted in the migrated source summary home | `generate_summary(project_id, source_id)` | `_prompt_config` lazy cache |
| `ai_services/chat_naming_service.py::ChatNamingService` | Class deleted in `chat/naming.py` | module functions | none |
| `ai_services/video_prompt_service.py::VideoPromptService` | Class deleted in the migrated video/media prompt home | module functions | none |

**Keep-as-class list:**
- `ClaudeService`: provider observability, streaming, retry/backoff, and broad call-site stability.
- `TaskService`: `ThreadPoolExecutor`, lock, futures dictionary, and cancellation state.
- `EnvService`: mutable `.env` writes and reload behavior.
- `ChatLoop`: migrated form of `MainChatService`; lazy tool definitions, iteration config, and agentic loop state.
- `MemoryService`: merge behavior and stateful memory policy.
- `PineconeService`: provider client lifecycle.
- All migrated stores from former `data_services/*`.
- All migrated `ai_agents/*`, studio orchestration classes, and source orchestration classes unless a later ticket names a specific stateless conversion.
- No subclasses were found in the audit; polymorphism is not a reason to keep or remove any class.

`NBB-706` emits no `move-plan.csv` rows. It is a verification and manual-cleanup ticket, not a mover. Running `mcp__refactory__validate_imports` on `backend/app/` at the end of the ticket is still expected, but no refactory execution tools are called.

**Out of scope:**
- Net-new features.
- Behavior changes unrelated to structure.
- Reopening renames already owned by upstream movement tickets.

**Acceptance criteria:**
- No forwarding-only modules or temporary shims remain without documented compatibility reason.
- Old mechanism taxonomy is gone from current guidance.
- New names use local context instead of ancestry repetition.
- The `NBB-704C` stateless-singleton allowlist is empty or contains only documented stateful exceptions.

**Verification:** Search for suffix drift, re-export barrels, legacy imports, and residual stateless singleton classes.

**Traceability:** Covers audit Phase 3a/3c/3e and final cleanup.
